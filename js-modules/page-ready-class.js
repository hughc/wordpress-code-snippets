/**
 * Module: Page Ready Class
 * Description: Adds a 'ready' class to the body when the page has fully loaded (jQuery ready handler).
 */

jQuery(document).ready(function ($) {
	$('body').addClass('ready');
});
